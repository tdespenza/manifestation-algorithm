/** Duration after which an inactive session is considered expired (30 days). */
export const SESSION_TIMEOUT_MS = 30 * 24 * 60 * 60 * 1000;

/** Inclusive bounds for a per-question user rating. */
export const MIN_RATING = 0;
export const MAX_RATING = 10;
